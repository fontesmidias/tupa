# Django
SECRET_KEY=change-me-in-prod
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Postgres (ADR-003)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/gestao_vagas_dev

# Redis (cache + dramatiq broker)
REDIS_URL=redis://localhost:6379/0

# Chave Fernet para criptografia de campos sensiveis (gerar com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())")
FIELD_ENCRYPTION_KEY=change-me-fernet-key

# DPO (Story 3.6b) — email monitorado para notificacoes LGPD criticas
DPO_EMAIL=

# Epic 4.0-MVP (Caminho C) — OpenAI GPT-4o-mini (provider ativo)
OPENAI_API_KEY=
OPENAI_MODEL_ID=gpt-4o-mini
OPENAI_PRICE_INPUT_PER_MTOKENS=0.15
OPENAI_PRICE_OUTPUT_PER_MTOKENS=0.60

# Anthropic (reservado — segundo driver, pos-H1)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL_ID=claude-sonnet-4-5
ANTHROPIC_PRICE_INPUT_PER_MTOKENS=3.0
ANTHROPIC_PRICE_OUTPUT_PER_MTOKENS=15.0

AI_LAB_ENABLED=False
